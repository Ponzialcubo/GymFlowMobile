import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Prueba inicial de GymFlow', () {
    // Un test vacío que siempre pasa para que no bloquee el compilador
    expect(true, true);
  });
}